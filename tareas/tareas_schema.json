{
  "bsonType": "object",
  "required": [
    "id",
    "titulo",
    "descripcion",
    "estado",
    "prioridad",
    "categoria",
    "fechaEntrega",
    "usuarioId",
    "fechaCreacion"
  ],
  "properties": {

    "id": {
      "bsonType": "int",
      "description": "Identificador de la tarea"
    },

    "titulo": {
      "bsonType": "string",
      "maxLength": 100
    },

    "descripcion": {
      "bsonType": "string"
    },

    "estado": {
      "enum": [
        "Pendiente",
        "EnProceso",
        "Completada"
      ]
    },

    "prioridad": {
      "enum": [
        "Alta",
        "Media",
        "Baja"
      ]
    },

    "categoria": {
      "bsonType": "string"
    },

    "fechaEntrega": {
      "bsonType": "date"
    },

    "usuarioId": {
      "bsonType": "int"
    },

    "fechaCreacion": {
      "bsonType": "date"
    }
  }
}